/*import React, { Component } from 'react';

class common extends Component {
    constructor(){
        super();
        this.isStringEmpty = this.this.isStringEmpty.bind(this);
    }

    isStringEmpty(str) {
        return (!str || str === undefined || str === "" || str.trim().length === 0);
    }
}
export default common;

isStringEmpty(str) {
    return (!str || str === undefined || str === "" || str.trim().length === 0);
}

*/
